var nombre = 'Alan', edad = 24

function imprimir () {
    console.log(`${nombre} tiene ${edad} años`) // Alan tiene 24 años
}

function imprimirEdad(n,e){
    console.log(`${n} tiene ${e} años`)
}